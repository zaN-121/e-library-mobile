import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    text:{
        textAlign: 'center',
        alignItems: 'center',
        fontSize: 24,
        color: 'sienna'
    },
    link:{
        textAlign: 'center',
        fontSize: 12,
        color: 'blue',
    },
})

export default styles