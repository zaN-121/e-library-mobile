import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    text:{
        textAlign:'center',
        alignItems: 'center',
        fontSize: 30,
        color: 'sienna',
        fontWeight: 'bold'
        
    },
    link:{
        textAlign: 'center',
        fontSize: 14,
        color: 'blue',
    },
})

export default styles;