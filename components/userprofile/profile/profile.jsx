import { SIZES, COLORS } from "../../../constants";
import { View, Text } from "react-native";

const userProfile = () => {
    return (
        <View>

        </View>
    )
}

export default userProfile